{
  "name": "backupmagic",
  "version": "0.4.1",
  "description": "cross-platform backup system",
  "main": "Sources/Application.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . backupMagic --platform=darwin --arch=x64 --app-version=0.4.1 --out=Dist --ignore=Dist --prune"
  },
  "author": "Vadim Brik",
  "license": "MIT",
  "dependencies": {
    "extract-zip": "^1.6.6",
    "jsftp": "^2.1.3",
    "zip-dir": "^1.0.2"
  },
  "devDependencies": {
    "electron": "^2.0.1"
  }
}
